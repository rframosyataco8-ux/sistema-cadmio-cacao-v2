global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: cadmio-backend
    metrics_path: /metrics
    scrape_interval: 10s
    scrape_timeout: 5s
    static_configs:
      - targets: ["backend:8000"]
        labels:
          service: cadmio-api
          env: docker

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
